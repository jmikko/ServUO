# Runs a D&D combat smoke test at boot (see Scripts/Misc/DnDCombatSelfTest.cs) and reports to the
# console. It spawns and deletes two mobiles in the live world, so leave it off on a real shard.
#Boolean Syntax: True | False
CombatSelfTest=False
